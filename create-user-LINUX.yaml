---
- name: Create User
  gather_facts: no
  hosts: all
  become: true
  tasks:
    - name: Add the user and Password , Assign to Group 
      user:
        name: '{{ var_user }}'
        group: '{{ var_group }}'
        password: '{{ var_password }}'
